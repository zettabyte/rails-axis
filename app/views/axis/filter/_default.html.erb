<%=
  select_tag axis_name(axis, "filter", filter_id, "type"),
    options_for_select(Axis::State::Filter.default_type_options(filter.attribute_type))
  %>
<%= text_field_tag axis_name(axis, "filter", filter_id, "value") %>
